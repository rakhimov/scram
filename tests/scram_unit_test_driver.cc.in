#include <iostream>
#include <string>
#include <stdio.h>
#include <unistd.h>

#include <gtest/gtest.h>

int main(int argc, char* argv[]) {
  const char* target = "@CMAKE_RUNTIME_OUTPUT_DIRECTORY@";
  assert(chdir(target) == 0);
  testing::InitGoogleTest(&argc, argv);
  return RUN_ALL_TESTS();
}
